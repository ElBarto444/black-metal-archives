{% extends 'base.html.twig' %}

{% block title %}Band{% endblock %}

{% block body %}
    <h1>Band</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ band.id }}</td>
            </tr>
            <tr>
                <th>BandName</th>
                <td>{{ band.bandName }}</td>
            </tr>
            <tr>
                <th>BandGenre</th>
                <td>{{ band.bandGenre }}</td>
            </tr>
            <tr>
                <th>BandPicture</th>
                <td>{{ band.bandPicture }}</td>
            </tr>
            <tr>
                <th>BandCountry</th>
                <td>{{ band.bandCountry }}</td>
            </tr>
            <tr>
                <th>BandYear</th>
                <td>{{ band.bandYear }}</td>
            </tr>
            <tr>
                <th>BandLogo</th>
                <td>{{ band.bandLogo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_band_index') }}">back to list</a>

    <a href="{{ path('app_band_edit', {'id': band.id}) }}">edit</a>

    {{ include('band/_delete_form.html.twig') }}
{% endblock %}
