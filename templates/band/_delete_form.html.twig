<form method="post" action="{{ path('app_band_delete', {'id': band.id}) }}" onsubmit="return confirm('Are you sure you want to delete this band?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ band.id) }}">
    <button class="btn border border-primary btn-lg">Delete</button>
</form>
